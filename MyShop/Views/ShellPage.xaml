<Page
    x:Class="MyShop.Pages.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1A1A2E">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Padding="20" Margin="0,20,0,30">
                    <TextBlock Text="🏪 MYSHOP" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Management System" FontSize="12" Foreground="#888888" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Spacing="5">
                    <TextBlock Text="MAIN MENU" FontSize="11" Foreground="#666666" Margin="20,0,0,10"/>

                    <Border x:Name="NavDashboard" Tag="Dashboard" 
                            PointerPressed="Nav_PointerPressed" 
                            Background="#7B5CD6" CornerRadius="8" Margin="10,0" Padding="15,12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="📊" FontSize="18"/>
                            <TextBlock Text="Dashboard" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border x:Name="NavProducts" Tag="Products" 
                            PointerPressed="Nav_PointerPressed"
                            Background="Transparent" CornerRadius="8" Margin="10,0" Padding="15,12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="📦" FontSize="18"/>
                            <TextBlock Text="Products" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border x:Name="NavOrders" Tag="Orders" 
                            PointerPressed="Nav_PointerPressed"
                            Background="Transparent" CornerRadius="8" Margin="10,0" Padding="15,12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="🛒" FontSize="18"/>
                            <TextBlock Text="Orders" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border x:Name="NavSettings" Tag="Settings" 
                            PointerPressed="Nav_PointerPressed"
                            Background="Transparent" CornerRadius="8" Margin="10,0" Padding="15,12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="⚙️" FontSize="18"/>
                            <TextBlock Text="Settings" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Border Grid.Row="2" Background="Transparent" CornerRadius="8" Margin="10,0,10,30" Padding="15,12">
                    <HyperlinkButton HorizontalAlignment="Stretch" Click="LogoutButton_Click" Padding="0">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="🚪" FontSize="18"/>
                            <TextBlock Text="Log Out" Foreground="#FF6B6B" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </HyperlinkButton>
                </Border>
            </Grid>
        </Border>

        <Frame x:Name="ContentFrame" Grid.Column="1" Background="White"/>
    </Grid>
</Page>