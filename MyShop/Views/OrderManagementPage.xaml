<Page
    x:Class="MyShop.Views.OrderManagementPage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:models="using:Database.models"
    mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="StatusBackgroundBrush" Color="{ThemeResource SystemAccentColorLight2}"/>
        <SolidColorBrush x:Key="StatusTextBrush" Color="{ThemeResource SystemAccentColorDark2}"/>
    </Page.Resources>

    <Grid RowSpacing="16" Padding="24" Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="16">
                <TextBlock Text="Quản lý Đơn hàng" Style="{StaticResource TitleTextBlockStyle}"/>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <CalendarDatePicker Header="Từ ngày" Date="{x:Bind ViewModel.FromDate, Mode=TwoWay}" Width="150"/>
                    <CalendarDatePicker Header="Đến ngày" Date="{x:Bind ViewModel.ToDate, Mode=TwoWay}" Width="150"/>

                    <Button Command="{x:Bind ViewModel.LoadDataCommand}" 
                            Style="{StaticResource AccentButtonStyle}" 
                            VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE71C;" FontSize="14"/>
                            <TextBlock Text="Lọc"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1" 
                    Command="{x:Bind ViewModel.GoToCreateOrderPageCommand}" 
                    VerticalAlignment="Bottom"
                    Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="Tạo đơn mới" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="1" Padding="16,12" Margin="0,0,12,0"
            Background="{ThemeResource LayerFillColorAltBrush}"
            CornerRadius="8,8,0,0"
            BorderThickness="1,1,1,0" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="MÃ ĐH" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="NGÀY TẠO" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <TextBlock Grid.Column="2" Text="THANH TOÁN" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <TextBlock Grid.Column="4" Text="TỔNG TIỀN" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="5" Text="TRẠNG THÁI" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
        </Grid>

        <ListView x:Name="OrderList"
                  Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Orders}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="OrderList_ItemClick"
                  Tag="{x:Bind ViewModel}">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,4"/>
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Order">
                    <Grid Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                          BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                          CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{x:Bind Id}" FontWeight="SemiBold" VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind CreatedAt, Converter={StaticResource DateTimeConverter}}" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE8C7;" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind PaymentMethod, Converter={StaticResource EnumConverter}}" 
                               TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <TextBlock Grid.Column="3" 
                                   Text="{x:Bind FinalPrice, Converter={StaticResource StringFormatConverter}, ConverterParameter='N0'}" 
                                   Foreground="{ThemeResource SystemFillColorSuccessBrush}" 
                                   FontWeight="Bold" FontSize="15"
                                   HorizontalAlignment="Right" VerticalAlignment="Center"/>

                        <Border Grid.Column="4" 
                                Background="{ThemeResource LayerFillColorDefaultBrush}" 
                                BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                                CornerRadius="12" Padding="12,4" 
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Status}" FontSize="12" FontWeight="SemiBold"/>
                        </Border>

                        <Button Grid.Column="5" 
                                Command="{Binding ElementName=OrderList, Path=Tag.DeleteOrderCommand}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource SubtleButtonStyle}"
                                Padding="8" Width="36" Height="36" CornerRadius="18"
                                ToolTipService.ToolTip="Xóa đơn hàng"
                                HorizontalAlignment="Right">
                            <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16" Margin="0,12,0,0">
            <Button Command="{x:Bind ViewModel.PreviousPageCommand}" Style="{StaticResource SubtleButtonStyle}">
                <FontIcon Glyph="&#xE76B;" FontSize="12"/>
            </Button>

            <TextBlock VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                Trang <Run Text="{x:Bind ViewModel.CurrentPage}" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/> 
                / <Run Text="{x:Bind ViewModel.TotalPages}"/>
            </TextBlock>

            <Button Command="{x:Bind ViewModel.NextPageCommand}" Style="{StaticResource SubtleButtonStyle}">
                <FontIcon Glyph="&#xE76C;" FontSize="12"/>
            </Button>
        </StackPanel>
    </Grid>
</Page>