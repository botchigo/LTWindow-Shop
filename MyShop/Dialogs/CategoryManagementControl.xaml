<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MyShop.Dialogs.CategoryManagementControl"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:models="using:Database.models"
    mc:Ignorable="d">

    <Grid RowSpacing="16" Padding="0,12,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quản lý Loại sản phẩm" Style="{StaticResource TitleTextBlockStyle}"/>

        <InfoBar Grid.Row="1" 
                 Severity="Error"
                 IsOpen="{x:Bind ViewModel.IsErrorOpen, Mode=TwoWay}"
                 Title="{x:Bind ViewModel.ErrorTitle, Mode=OneWay}"
                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                 IsClosable="True" />

        <StackPanel Grid.Row="2" Spacing="12" 
                    Background="{ThemeResource LayerFillColorDefaultBrush}" 
                    Padding="16" CornerRadius="8" BorderThickness="1" 
                    BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">

            <TextBlock Text="Thêm loại mới" FontWeight="SemiBold"/>

            <TextBox Header="Tên loại (*)" 
                     PlaceholderText="Ví dụ: Laptop Gaming" 
                     Text="{x:Bind ViewModel.NewCategoryName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBox Header="Mô tả" 
                     PlaceholderText="Mô tả ngắn gọn..." 
                     Text="{x:Bind ViewModel.NewCategoryDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Command="{x:Bind ViewModel.AddCategoryCommand}" 
                    Style="{StaticResource AccentButtonStyle}" 
                    HorizontalAlignment="Stretch" Margin="0,4,0,0">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Add"/>
                    <TextBlock Text="Thêm danh mục"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Grid Grid.Row="3" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Danh sách hiện có" Style="{StaticResource BodyStrongTextBlockStyle}"/>

            <Border Grid.Row="1" 
            BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" 
            CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}">

                <ListView x:Name="CategoryList"
                            Tag="{x:Bind ViewModel}"
                            ItemsSource="{x:Bind ViewModel.Categories}" 
                            SelectionMode="None"
                            Padding="4"
                            ScrollViewer.VerticalScrollBarVisibility="Auto" 
                            ScrollViewer.VerticalScrollMode="Enabled">

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Category">
                            <Grid Padding="12,8" ColumnSpacing="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <FontIcon Glyph="&#xE8F1;" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind Description}" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                       TextTrimming="CharacterEllipsis" MaxLines="1"
                                       Visibility="{x:Bind Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>

                                <Button Grid.Column="2" 
        Background="Transparent" BorderThickness="0" Padding="8">

                                    <FontIcon Glyph="&#xE74D;" FontSize="16" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                    <ToolTipService.ToolTip>Xóa loại này</ToolTipService.ToolTip>

                                    <Button.Flyout>
                                        <Flyout Placement="BottomEdgeAlignedRight">
                                            <StackPanel Spacing="12" Width="200">
                                                <TextBlock Text="Bạn có chắc chắn muốn xóa loại này không?" 
                           TextWrapping="Wrap" FontWeight="SemiBold"/>

                                                <Button Content="Xóa ngay" 
                        HorizontalAlignment="Stretch"
                        Background="{ThemeResource SystemFillColorCriticalBrush}"
                        Foreground="White"
                        
                        
                        Command="{Binding ElementName=CategoryList, Path=Tag.DeleteCategoryCommand}"
                        CommandParameter="{Binding}"/>

                                            </StackPanel>
                                        </Flyout>
                                    </Button.Flyout>

                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </Grid>

</UserControl>
