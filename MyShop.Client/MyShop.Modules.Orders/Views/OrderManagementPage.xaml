<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Modules.Orders.Views.OrderManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Modules.Orders.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:graphql="using:MyShop.Infrastructure"
    mc:Ignorable="d"
    NavigationCacheMode="Disabled">

    <Grid RowSpacing="16" Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="16">
                <TextBlock Text="Quản lý Đơn hàng" Style="{StaticResource TitleTextBlockStyle}"/>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <CalendarDatePicker Header="Từ ngày" Date="{x:Bind ViewModel.FromDate, Mode=TwoWay}" Width="150"/>
                    <CalendarDatePicker Header="Đến ngày" Date="{x:Bind ViewModel.ToDate, Mode=TwoWay}" Width="150"/>

                    <Button Command="{x:Bind ViewModel.LoadDataCommand}" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE71C;" FontSize="14"/>
                            <TextBlock Text="Lọc"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1" Command="{x:Bind ViewModel.GoToCreateOrderPageCommand}" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="Tạo đơn mới" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="1" Padding="16,12" Margin="0,0,12,0" Background="{ThemeResource LayerFillColorAltBrush}" CornerRadius="8,8,0,0" BorderThickness="1,1,1,0" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="140"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="MÃ ĐH" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="NGÀY TẠO" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="2" Text="THANH TOÁN" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Grid.Column="3" Text="TỔNG TIỀN" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="4" Text="TRẠNG THÁI" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
        </Grid>

        <ListView x:Name="OrderList" Grid.Row="2" ItemsSource="{x:Bind ViewModel.Orders, Mode=OneWay}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="OrderList_ItemClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,4"/>
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="graphql:IGetOrders_Orders_Items">
                    <Grid Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="140"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{x:Bind Id}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind CreatedAt, Converter={StaticResource DateTimeConverter}}" VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE8C7;" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind PaymentMethod, Converter={StaticResource EnumToStringConverter}}"/>
                        </StackPanel>

                        <TextBlock Grid.Column="3" Text="{x:Bind FinalPrice, Converter={StaticResource CurrencyConverter}}" Foreground="{ThemeResource SystemFillColorSuccessBrush}" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>

                        <Border Grid.Column="4" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" CornerRadius="12" Padding="12,4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Status, Converter={StaticResource EnumToStringConverter}}" FontSize="12" FontWeight="SemiBold"/>
                        </Border>
                       
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.RowSpan="4" Background="#80000000" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing IsActive="True"/>
        </Grid>
    </Grid>
</Page>
