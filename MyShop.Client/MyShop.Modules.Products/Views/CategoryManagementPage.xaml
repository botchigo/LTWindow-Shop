<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Modules.Products.Views.CategoryManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Modules.Products.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:graphql="using:MyShop.Infrastructure"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Padding="24,24,24,12">
            <Button Command="{x:Bind ViewModel.GoBackCommand}" Style="{StaticResource NavigationBackButtonNormalStyle}"/>
            <TextBlock Text="Quản lý Loại sản phẩm" Style="{StaticResource TitleTextBlockStyle}"/>
        </StackPanel>

        <Grid Grid.Row="1" Padding="24,0,24,24" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="16" VerticalAlignment="Top"
                        Background="{ThemeResource LayerFillColorDefaultBrush}" 
                        Padding="24" CornerRadius="8" BorderThickness="1" 
                        BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">

                <TextBlock Text="Thêm loại mới" Style="{StaticResource SubtitleTextBlockStyle}"/>

                <TextBox Header="Tên loại (*)" 
                         PlaceholderText="Ví dụ: Laptop Gaming" 
                         Text="{x:Bind ViewModel.NewCategoryName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBox Header="Mô tả" 
                         PlaceholderText="Mô tả ngắn gọn..." 
                         Text="{x:Bind ViewModel.NewCategoryDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>

                <Button Command="{x:Bind ViewModel.AddCategoryCommand}" 
                        Style="{StaticResource AccentButtonStyle}" 
                        HorizontalAlignment="Stretch" Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Add"/>
                        <TextBlock Text="Thêm danh mục"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <Grid Grid.Column="1" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Danh sách hiện có" Style="{StaticResource BodyStrongTextBlockStyle}"/>

                <ListView Grid.Row="1" 
                          ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}" 
                          SelectionMode="None"
                          BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" 
                          CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}">

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="graphql:IGetCategories_Categories_Items">
                            <Grid Padding="16,12" ColumnSpacing="16" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <FontIcon Glyph="&#xE8F1;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" FontFamily="{StaticResource SymbolThemeFontFamily}"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="15"/>
                                    <TextBlock Text="{x:Bind Description}" FontSize="12" 
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                               TextTrimming="CharacterEllipsis" MaxLines="1"
                                               Visibility="{x:Bind Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>

                                <Button Grid.Column="2" Background="Transparent" BorderThickness="0" Padding="8"
                                        Click="DeleteCategory_Click" 
                                        ToolTipService.ToolTip="Xóa loại này">

                                    <FontIcon Glyph="&#xE74D;" FontSize="16" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>

        <Grid Grid.RowSpan="2" Background="#80000000" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing IsActive="True"/>
        </Grid>
    </Grid>
</Page>
