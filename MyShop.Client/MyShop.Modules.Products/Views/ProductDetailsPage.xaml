<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Modules.Products.Views.ProductDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Modules.Products.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent" IsOpen="False">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="12" Margin="12,10">
                    <Button Command="{x:Bind ViewModel.GoBackCommand}" Style="{StaticResource NavigationBackButtonNormalStyle}"/>
                    <TextBlock Text="Chi tiết sản phẩm" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
                </StackPanel>
            </CommandBar.Content>

            <AppBarButton Icon="Edit" Label="Cập nhật" Command="{x:Bind ViewModel.GoToEditCommand}"/>
            <AppBarButton Icon="Delete" Label="Xóa" Command="{x:Bind ViewModel.DeleteProductCommand}" Foreground="Red"/>
        </CommandBar>

        <ProgressRing Grid.Row="1" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" 
                      Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                      HorizontalAlignment="Center" VerticalAlignment="Center" Canvas.ZIndex="10"/>

        <ScrollViewer Grid.Row="1" Padding="24">
            <StackPanel Spacing="24" MaxWidth="1000" HorizontalAlignment="Left">

                <Grid ColumnSpacing="32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="400"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="12">

                        <Border CornerRadius="8" BorderThickness="1" 
                                BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" 
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

                            <FlipView x:Name="ImageFlipView" 
                                      ItemsSource="{x:Bind ViewModel.Product.ProductImages, Mode=OneWay}" 
                                      Height="300">
                                <FlipView.ItemTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Path, Converter={StaticResource PathToImageConverter}}" 
                                                Stretch="Uniform"/>
                                    </DataTemplate>
                                </FlipView.ItemTemplate>
                            </FlipView>
                        </Border>

                        <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                                      VerticalScrollBarVisibility="Disabled" 
                                      HorizontalScrollMode="Auto" 
                                      VerticalScrollMode="Disabled">

                            <GridView ItemsSource="{x:Bind ViewModel.Product.ProductImages, Mode=OneWay}"
                                      SelectedIndex="{x:Bind ImageFlipView.SelectedIndex, Mode=TwoWay}"
                                      SelectionMode="Single"
                                      HorizontalAlignment="Center">

                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="60" Height="60" CornerRadius="4" Margin="4"
                                                BorderThickness="1" 
                                                BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
                                            <Image Source="{Binding Path, Converter={StaticResource PathToImageConverter}}" 
                                                    Stretch="UniformToFill"/>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>

                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>
                        </ScrollViewer>

                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="16">
                        <Border Background="{ThemeResource AccentFillColorDefaultBrush}" HorizontalAlignment="Left" CornerRadius="4" Padding="8,4">
                            <TextBlock Text="{x:Bind ViewModel.Product.Category.Name, Mode=OneWay}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                        </Border>

                        <TextBlock Text="{x:Bind ViewModel.Product.Name, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                            <TextBlock Text="{x:Bind ViewModel.Product.SalePrice, Mode=OneWay, Converter={StaticResource CurrencyConverter}}" 
                                       Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource SystemFillColorSuccessBrush}" FontSize="24"/>
                            <TextBlock Text="VND" VerticalAlignment="Bottom" Margin="0,0,0,6" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <MenuFlyoutSeparator/>

                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Giá nhập:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind ViewModel.Product.ImportPrice, Mode=OneWay, Converter={StaticResource CurrencyConverter}}"/>

                            <TextBlock Grid.Row="1" Text="Tồn kho:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.Product.Stock, Mode=OneWay}" FontWeight="Bold"/>
                                <TextBlock Text="sản phẩm" Foreground="Gray"/>
                            </StackPanel>

                            <TextBlock Grid.Row="2" Text="Đã bán:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind ViewModel.Product.SaleAmount, Mode=OneWay}" FontWeight="SemiBold"/>

                            <TextBlock Grid.Row="3" Text="Mã SKU:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{x:Bind ViewModel.Product.Sku, Mode=OneWay}" FontFamily="Consolas"/>

                        </Grid>
                        <StackPanel Spacing="8">
                            <TextBlock Text="Mô tả sản phẩm" Style="{StaticResource SubtitleTextBlockStyle}"/>

                            <Border Background="{ThemeResource LayerFillColorDefaultBrush}" 
                            Padding="16" CornerRadius="8"
                            BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
                                <TextBlock Text="{x:Bind ViewModel.Product.Description, Mode=OneWay}" 
                                   TextWrapping="Wrap" 
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   LineHeight="22"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
