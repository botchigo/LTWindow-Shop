query GetProductNamesUpdated {
  orders(
    where: { orderItems: { any: true } }
    order: { id: ASC }
    take: 20
  ) {
    items {
        orderItems{
            product{
                id,
                name
            }
        }
    }
  }
}